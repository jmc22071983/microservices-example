spring:
  profiles: default
  application:
    name: breweries-service
  cloud:
    config:
      username: admin
      password: password
      uri: http://localhost:8888
      
---

spring:
  profiles: swarm
  application:
    name: breweries-service
  cloud:
    config:
      username: admin
      password: password
      uri: http://config:8888

---
spring:
  profiles: aws
  application:
    name: breweries-service
  cloud:
    config:
      uri: http://cloud.r53config.vpc
