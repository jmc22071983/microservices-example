spring:
  profiles: default
  application:
    name: eureka
  cloud:
    config:
      username: admin
      password: password
      uri: http://localhost:8888
---

spring:
  profiles: swarm
  application:
    name: eureka
  cloud:
    config:
      uri: http://config:8888
      username: admin
      password: password

---
spring:
  profiles: aws
  application:
    name: eureka
  cloud:
    config:
      uri: http://cloud.r53config.vpc
      username: admin
      password: password