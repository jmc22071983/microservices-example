# Spring properties
spring:
  application:
    name: eureka-client
# HTTP Server
server:
  port: 8098
# Eureka Server Access
eureka:
  enabled: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
