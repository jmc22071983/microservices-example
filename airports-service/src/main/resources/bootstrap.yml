spring:
  profiles: default
  application:
    name: airports-service
  cloud:
    config:
      uri: http://localhost:8888
      
---

spring:
  profiles: swarm
  application:
    name: airports-service
  cloud:
    config:
      uri: http://config:8888

---
spring:
  profiles: aws
  application:
    name: airports-service
  cloud:
    config:
      uri: http://cloud.r53config.vpc
